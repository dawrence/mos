
<%= form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
  <div>
 		<%= f.email_field :email, :autofocus => true, placeholder: "Email", class: "radius" %>
 	</div>

  <div>
  	<%= f.password_field :password, placeholder: "Contraseña", class: "radius" %>
 	</div>

  <% if devise_mapping.rememberable? -%>
    <div><%= f.check_box :remember_me %> <%= f.label :remember_me, "Recordarme" %></div>
  <% end -%>

  <div><%= f.submit "Ingresar", class: "button success" %></div>
	<%= render "devise/shared/links" %>
<% end %>

